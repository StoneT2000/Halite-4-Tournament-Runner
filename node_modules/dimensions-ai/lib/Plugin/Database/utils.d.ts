import { Database } from './index';
/**
 * Generate a jwt for a user
 * @param user
 */
export declare function generateToken(user: Database.User): string;
/**
 * Verify a jwt
 * @param token - the jwt to verify
 */
export declare function verify(token: string): Promise<any>;
