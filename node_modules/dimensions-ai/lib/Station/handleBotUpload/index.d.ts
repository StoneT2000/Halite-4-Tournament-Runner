import express from 'express';
import { Database } from '../../Plugin/Database';
export interface UploadData {
    file: string;
    name: string;
    playerID: string;
    botdir: string;
    originalFile: string;
}
/**
 * Returns path to unzipped bot contents and the main file. If user provided, will only handle upload if user
 * matches playerID given
 */
export declare const handleBotUpload: (req: express.Request<import("express-serve-static-core").ParamsDictionary>, user?: Database.PublicUser) => Promise<UploadData[]>;
