"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const mathematics_1 = require("./mathematics");
class Rating extends mathematics_1.SkillGaussian {
    constructor(mu = 25, sigma) {
        if (Array.isArray(mu)) {
            [mu, sigma] = mu;
        }
        else if (mu instanceof mathematics_1.SkillGaussian) {
            sigma = mu.sigma;
            mu = mu.mu;
        }
        if (sigma === undefined) {
            sigma = mu / 3;
        }
        super(mu, sigma);
    }
    toString() {
        const mu = this.mu.toFixed(3);
        const sigma = this.sigma.toFixed(3);
        return `Rating(mu=${mu}, sigma=${sigma})`;
    }
}
exports.Rating = Rating;
//# sourceMappingURL=rating.js.map